import 'package:flutter/material.dart';

class ExampleView extends StatelessWidget {
  const ExampleView({super.key,required this.toDo});

  final void Function() toDo;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
